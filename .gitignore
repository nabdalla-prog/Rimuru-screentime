.claude/
*.corrupt-*
node_modules/
__pycache__/
# Pictures chosen for the background belong on the user's machine, never in this repo.
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.gif
# The one root image that belongs here: the marketplace/README preview (made-up data).
!/preview.png
*.pyc
