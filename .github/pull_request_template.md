## What this changes

## Checklist

- [ ] Tests pass (`node --test tests/*.test.js` and `python3 -m unittest discover -s tests`), and I added tests for logic changes
- [ ] The plugin still makes no network requests and writes only to its own files
- [ ] No third-party artwork was added; screenshots (if any) use made-up data
- [ ] For a release: version bumped in `manifest.json` and `js/Model.js`, and `CHANGELOG.md` updated
